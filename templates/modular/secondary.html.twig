<div class="bg-secondary p-8">
    {% set image = page.media.images|first %}
    {% if image %}
        {{ image.cropResize(200,200).html('','','float-right shadow-md dark:border-black border-8 rounded')|raw }}
    {% endif %}
{{ content|raw }}
</div>